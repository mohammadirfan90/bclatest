module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/events [external] (events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("events", () => require("events"));

module.exports = mod;
}),
"[externals]/process [external] (process, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("process", () => require("process"));

module.exports = mod;
}),
"[externals]/net [external] (net, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("net", () => require("net"));

module.exports = mod;
}),
"[externals]/tls [external] (tls, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("tls", () => require("tls"));

module.exports = mod;
}),
"[externals]/timers [external] (timers, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("timers", () => require("timers"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[externals]/buffer [external] (buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("buffer", () => require("buffer"));

module.exports = mod;
}),
"[externals]/string_decoder [external] (string_decoder, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("string_decoder", () => require("string_decoder"));

module.exports = mod;
}),
"[externals]/crypto [external] (crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("crypto", () => require("crypto"));

module.exports = mod;
}),
"[externals]/zlib [external] (zlib, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("zlib", () => require("zlib"));

module.exports = mod;
}),
"[externals]/util [external] (util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("util", () => require("util"));

module.exports = mod;
}),
"[externals]/url [external] (url, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("url", () => require("url"));

module.exports = mod;
}),
"[externals]/fs [external] (fs, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("fs", () => require("fs"));

module.exports = mod;
}),
"[externals]/path [external] (path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("path", () => require("path"));

module.exports = mod;
}),
"[project]/src/lib/db.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "callProcedure",
    ()=>callProcedure,
    "checkConnection",
    ()=>checkConnection,
    "closePool",
    ()=>closePool,
    "execute",
    ()=>execute,
    "getPool",
    ()=>getPool,
    "query",
    ()=>query,
    "queryOne",
    ()=>queryOne,
    "withTransaction",
    ()=>withTransaction
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f2e$pnpm$2f$mysql2$40$3$2e$16$2e$0$2f$node_modules$2f$mysql2$2f$promise$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/.pnpm/mysql2@3.16.0/node_modules/mysql2/promise.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/fs [external] (fs, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/path [external] (path, cjs)");
;
;
;
// =============================================================================
// Database Configuration
// =============================================================================
// Load SSL certificate for Azure MySQL
const getSSLConfig = ()=>{
    if (process.env.DATABASE_SSL !== 'true') return undefined;
    const certPath = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'cert', 'DigiCertGlobalRootCA.crt');
    try {
        if (__TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].existsSync(certPath)) {
            return {
                ca: __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].readFileSync(certPath),
                rejectUnauthorized: false
            };
        }
    } catch  {
        console.warn('[DB] SSL certificate not found, using default SSL config');
    }
    return {
        rejectUnauthorized: false
    };
};
const poolConfig = {
    host: process.env.DATABASE_HOST || 'localhost',
    port: parseInt(process.env.DATABASE_PORT || '3306'),
    user: process.env.DATABASE_USER || 'root',
    password: process.env.DATABASE_PASSWORD || '',
    database: process.env.DATABASE_NAME || 'bnkcore',
    waitForConnections: true,
    connectionLimit: parseInt(process.env.DATABASE_POOL_MAX || '10'),
    queueLimit: 0,
    enableKeepAlive: true,
    keepAliveInitialDelay: 0,
    ssl: getSSLConfig()
};
// =============================================================================
// Connection Pool (Singleton)
// =============================================================================
let pool = null;
function getPool() {
    if (!pool) {
        pool = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f2e$pnpm$2f$mysql2$40$3$2e$16$2e$0$2f$node_modules$2f$mysql2$2f$promise$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].createPool(poolConfig);
        // Log pool events in development
        if (("TURBOPACK compile-time value", "development") === 'development' && process.env.ENABLE_QUERY_LOGGING === 'true') {
            pool.on('connection', ()=>{
                console.log('[DB] New connection established');
            });
            pool.on('release', ()=>{
                console.log('[DB] Connection released');
            });
        }
    }
    return pool;
}
async function query(sql, params) {
    const pool = getPool();
    const [rows] = await pool.query(sql, params);
    return rows;
}
async function queryOne(sql, params) {
    const rows = await query(sql, params);
    return rows[0] || null;
}
async function execute(sql, params) {
    const pool = getPool();
    const [result] = await pool.execute(sql, params);
    return result;
}
async function withTransaction(callback) {
    const pool = getPool();
    const connection = await pool.getConnection();
    try {
        await connection.beginTransaction();
        const result = await callback(connection);
        await connection.commit();
        return result;
    } catch (error) {
        await connection.rollback();
        throw error;
    } finally{
        connection.release();
    }
}
async function callProcedure(procedureName, inParams, outParamNames) {
    const pool = getPool();
    const connection = await pool.getConnection();
    try {
        // Build the CALL statement with parameter placeholders
        const inPlaceholders = inParams.map(()=>'?').join(', ');
        const outPlaceholders = outParamNames.map((name)=>`@${name}`).join(', ');
        const allPlaceholders = [
            inPlaceholders,
            outPlaceholders
        ].filter(Boolean).join(', ');
        const callSql = `CALL ${procedureName}(${allPlaceholders})`;
        // Execute the procedure
        const [results] = await connection.query(callSql, inParams);
        // Get output parameters
        const outParams = {};
        if (outParamNames.length > 0) {
            const selectOutSql = `SELECT ${outParamNames.map((name)=>`@${name} AS ${name}`).join(', ')}`;
            const [outRows] = await connection.query(selectOutSql);
            if (outRows[0]) {
                Object.assign(outParams, outRows[0]);
            }
        }
        // Handle multiple result sets from stored procedure
        const resultSets = Array.isArray(results) ? results : [
            results
        ];
        const dataResults = resultSets.filter((r)=>Array.isArray(r) && r.length > 0 && !('affectedRows' in r[0]));
        return {
            results: dataResults,
            outParams
        };
    } finally{
        connection.release();
    }
}
async function checkConnection() {
    try {
        const pool = getPool();
        const connection = await pool.getConnection();
        await connection.ping();
        connection.release();
        return true;
    } catch  {
        return false;
    }
}
async function closePool() {
    if (pool) {
        await pool.end();
        pool = null;
    }
}
}),
"[project]/src/lib/services/analytics-service.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "generateMonthlyAggregates",
    ()=>generateMonthlyAggregates,
    "getAvailableDates",
    ()=>getAvailableDates,
    "getAvailablePeriods",
    ()=>getAvailablePeriods,
    "getDailySystemTotals",
    ()=>getDailySystemTotals,
    "getDailyTotals",
    ()=>getDailyTotals,
    "getDailyTransactionSummary",
    ()=>getDailyTransactionSummary,
    "getMonthlySummaries",
    ()=>getMonthlySummaries,
    "getMonthlySystemTotals",
    ()=>getMonthlySystemTotals,
    "getTopAccounts",
    ()=>getTopAccounts,
    "rebuildAnalytics",
    ()=>rebuildAnalytics
]);
/**
 * Analytics Service
 * Feature 15: Financial Analytics, Management Reports & Materialized Aggregates
 * 
 * Provides functions to query analytics tables and trigger aggregate generation.
 * Analytics data is derived from the ledger and must never be treated as source of truth.
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/db.ts [app-route] (ecmascript)");
;
async function getDailyTotals(date, options = {}) {
    const { accountId, page = 1, size = 50 } = options;
    const offset = (page - 1) * size;
    let whereClause = 'WHERE dat.date = ?';
    const params = [
        date
    ];
    if (accountId) {
        whereClause += ' AND dat.account_id = ?';
        params.push(accountId);
    }
    // Get count
    const countResult = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["queryOne"])(`SELECT COUNT(*) as count 
         FROM daily_account_totals dat 
         ${whereClause}`, params);
    // Get data
    const rows = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["query"])(`SELECT dat.id, dat.account_id, a.account_number,
                CONCAT(c.first_name, ' ', c.last_name) as customer_name,
                dat.date, dat.opening_balance, dat.closing_balance,
                dat.total_debits, dat.total_credits,
                dat.debit_count, dat.credit_count
         FROM daily_account_totals dat
         INNER JOIN accounts a ON a.id = dat.account_id
         INNER JOIN customers c ON c.id = a.customer_id
         ${whereClause}
         ORDER BY dat.closing_balance DESC
         LIMIT ? OFFSET ?`, [
        ...params,
        size,
        offset
    ]);
    return {
        totals: rows.map((row)=>({
                id: row.id,
                accountId: row.account_id,
                accountNumber: row.account_number,
                customerName: row.customer_name,
                date: row.date.toISOString().split('T')[0],
                openingBalance: parseFloat(row.opening_balance),
                closingBalance: parseFloat(row.closing_balance),
                totalDebits: parseFloat(row.total_debits),
                totalCredits: parseFloat(row.total_credits),
                debitCount: row.debit_count,
                creditCount: row.credit_count
            })),
        total: countResult?.count || 0
    };
}
async function getDailySystemTotals(date) {
    const row = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["queryOne"])(`SELECT 
            COUNT(DISTINCT dat.account_id) as total_accounts,
            COUNT(DISTINCT CASE WHEN a.status = 'ACTIVE' THEN a.id END) as total_active_accounts,
            COALESCE(SUM(dat.total_debits + dat.total_credits), 0) as total_volume,
            COALESCE(SUM(dat.total_credits), 0) as total_deposits,
            COALESCE(SUM(dat.total_debits), 0) as total_withdrawals,
            COALESCE(SUM(dat.debit_count + dat.credit_count), 0) as total_transactions,
            COALESCE(AVG(dat.closing_balance), 0) as avg_account_balance
         FROM daily_account_totals dat
         INNER JOIN accounts a ON a.id = dat.account_id
         WHERE dat.date = ?`, [
        date
    ]);
    return {
        totalAccounts: row?.total_accounts || 0,
        totalActiveAccounts: row?.total_active_accounts || 0,
        totalVolume: parseFloat(row?.total_volume || '0'),
        totalDeposits: parseFloat(row?.total_deposits || '0'),
        totalWithdrawals: parseFloat(row?.total_withdrawals || '0'),
        totalTransactions: row?.total_transactions || 0,
        avgAccountBalance: parseFloat(row?.avg_account_balance || '0')
    };
}
async function getDailyTransactionSummary(date) {
    const row = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["queryOne"])(`SELECT 
            COALESCE(SUM(CASE WHEN entry_type = 'CREDIT' THEN amount ELSE 0 END), 0) as total_deposits,
            COALESCE(SUM(CASE WHEN entry_type = 'DEBIT' THEN amount ELSE 0 END), 0) as total_withdrawals,
            COUNT(DISTINCT transaction_id) as transaction_count,
            COALESCE(SUM(amount), 0) as volume
         FROM ledger_entries
         WHERE entry_date = ?`, [
        date
    ]);
    const result = {
        date,
        totalDeposits: parseFloat(row?.total_deposits || '0'),
        totalWithdrawals: parseFloat(row?.total_withdrawals || '0'),
        transactionCount: row?.transaction_count || 0,
        volume: parseFloat(row?.volume || '0')
    };
    // DEBUG: Write to a file since terminal output is hard to capture
    try {
        const fs = __turbopack_context__.r("[externals]/fs [external] (fs, cjs)");
        const logMsg = `[${new Date().toISOString()}] getDailyTransactionSummary date=${date} result=${JSON.stringify(result)}\n`;
        fs.appendFileSync('service_debug.log', logMsg);
    } catch (e) {}
    return result;
}
async function getMonthlySummaries(year, month, options = {}) {
    const { accountId, page = 1, size = 50 } = options;
    const offset = (page - 1) * size;
    let whereClause = 'WHERE mas.year = ? AND mas.month = ?';
    const params = [
        year,
        month
    ];
    if (accountId) {
        whereClause += ' AND mas.account_id = ?';
        params.push(accountId);
    }
    // Get count
    const countResult = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["queryOne"])(`SELECT COUNT(*) as count 
         FROM monthly_account_summaries mas 
         ${whereClause}`, params);
    // Get data
    const rows = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["query"])(`SELECT mas.id, mas.account_id, a.account_number,
                CONCAT(c.first_name, ' ', c.last_name) as customer_name,
                mas.year, mas.month, mas.opening_balance, mas.closing_balance,
                mas.total_debits, mas.total_credits,
                mas.debit_count, mas.credit_count,
                mas.avg_daily_balance, mas.interest_earned, mas.fees_charged
         FROM monthly_account_summaries mas
         INNER JOIN accounts a ON a.id = mas.account_id
         INNER JOIN customers c ON c.id = a.customer_id
         ${whereClause}
         ORDER BY mas.closing_balance DESC
         LIMIT ? OFFSET ?`, [
        ...params,
        size,
        offset
    ]);
    return {
        summaries: rows.map((row)=>({
                id: row.id,
                accountId: row.account_id,
                accountNumber: row.account_number,
                customerName: row.customer_name,
                year: row.year,
                month: row.month,
                openingBalance: parseFloat(row.opening_balance),
                closingBalance: parseFloat(row.closing_balance),
                totalDebits: parseFloat(row.total_debits),
                totalCredits: parseFloat(row.total_credits),
                debitCount: row.debit_count,
                creditCount: row.credit_count,
                avgDailyBalance: parseFloat(row.avg_daily_balance),
                interestEarned: parseFloat(row.interest_earned),
                feesCharged: parseFloat(row.fees_charged)
            })),
        total: countResult?.count || 0
    };
}
async function getMonthlySystemTotals(year, month) {
    const row = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["queryOne"])(`SELECT 
            COUNT(DISTINCT mas.account_id) as total_accounts,
            COUNT(DISTINCT CASE WHEN a.status = 'ACTIVE' THEN a.id END) as total_active_accounts,
            COALESCE(SUM(mas.total_debits + mas.total_credits), 0) as total_volume,
            COALESCE(SUM(mas.total_credits), 0) as total_deposits,
            COALESCE(SUM(mas.total_debits), 0) as total_withdrawals,
            COALESCE(SUM(mas.debit_count + mas.credit_count), 0) as total_transactions,
            COALESCE(AVG(mas.avg_daily_balance), 0) as avg_account_balance
         FROM monthly_account_summaries mas
         INNER JOIN accounts a ON a.id = mas.account_id
         WHERE mas.year = ? AND mas.month = ?`, [
        year,
        month
    ]);
    return {
        totalAccounts: row?.total_accounts || 0,
        totalActiveAccounts: row?.total_active_accounts || 0,
        totalVolume: parseFloat(row?.total_volume || '0'),
        totalDeposits: parseFloat(row?.total_deposits || '0'),
        totalWithdrawals: parseFloat(row?.total_withdrawals || '0'),
        totalTransactions: row?.total_transactions || 0,
        avgAccountBalance: parseFloat(row?.avg_account_balance || '0')
    };
}
async function getTopAccounts(year, month, category) {
    let whereClause = 'WHERE tam.year = ? AND tam.month = ?';
    const params = [
        year,
        month
    ];
    if (category) {
        whereClause += ' AND tam.category = ?';
        params.push(category);
    }
    const rows = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["query"])(`SELECT tam.rank_position, tam.account_id, a.account_number,
                CONCAT(c.first_name, ' ', c.last_name) as customer_name,
                tam.category, tam.metric_value
         FROM top_accounts_monthly tam
         INNER JOIN accounts a ON a.id = tam.account_id
         INNER JOIN customers c ON c.id = a.customer_id
         ${whereClause}
         ORDER BY tam.category, tam.rank_position`, params);
    return rows.map((row)=>({
            rank: row.rank_position,
            accountId: row.account_id,
            accountNumber: row.account_number,
            customerName: row.customer_name,
            category: row.category,
            metricValue: parseFloat(row.metric_value)
        }));
}
async function generateMonthlyAggregates(year, month, userId) {
    // Call the stored procedure
    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["execute"])(`CALL sp_generate_monthly_aggregates(?, ?, ?, @accounts_processed, @status, @message)`, [
        year,
        month,
        userId
    ]);
    // Get output parameters
    const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["queryOne"])(`SELECT @accounts_processed as accounts_processed, @status as status, @message as message`);
    return {
        accountsProcessed: result?.accounts_processed || 0,
        status: result?.status || 'UNKNOWN',
        message: result?.message || ''
    };
}
async function rebuildAnalytics(userId) {
    // Call the stored procedure
    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["execute"])(`CALL sp_rebuild_analytics(?, @daily_rows, @monthly_rows, @status, @message)`, [
        userId
    ]);
    // Get output parameters
    const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["queryOne"])(`SELECT @daily_rows as daily_rows, @monthly_rows as monthly_rows, @status as status, @message as message`);
    return {
        dailyRows: result?.daily_rows || 0,
        monthlyRows: result?.monthly_rows || 0,
        status: result?.status || 'UNKNOWN',
        message: result?.message || ''
    };
}
async function getAvailablePeriods() {
    const rows = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["query"])(`SELECT DISTINCT year, month 
         FROM monthly_account_summaries 
         ORDER BY year DESC, month DESC 
         LIMIT 24`);
    return rows.map((row)=>({
            year: row.year,
            month: row.month
        }));
}
async function getAvailableDates(limit = 30) {
    const rows = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["query"])(`SELECT DISTINCT date 
         FROM daily_account_totals 
         ORDER BY date DESC 
         LIMIT ?`, [
        limit
    ]);
    return rows.map((row)=>row.date.toISOString().split('T')[0]);
}
}),
"[project]/src/app/api/v1/debug/reports/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "GET",
    ()=>GET
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f2e$pnpm$2f$next$40$16$2e$1$2e$1_$40$babel$2b$core$40$7$2e$2_cd686fafa065e16de73e229f5879b04d$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/.pnpm/next@16.1.1_@babel+core@7.2_cd686fafa065e16de73e229f5879b04d/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$services$2f$analytics$2d$service$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/services/analytics-service.ts [app-route] (ecmascript)");
;
;
async function GET(request) {
    const { searchParams } = new URL(request.url);
    const date = searchParams.get('date') || '2026-01-17';
    try {
        const liveSummary = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$services$2f$analytics$2d$service$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getDailyTransactionSummary"])(date);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f2e$pnpm$2f$next$40$16$2e$1$2e$1_$40$babel$2b$core$40$7$2e$2_cd686fafa065e16de73e229f5879b04d$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            date,
            liveSummary
        });
    } catch (error) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f2e$pnpm$2f$next$40$16$2e$1$2e$1_$40$babel$2b$core$40$7$2e$2_cd686fafa065e16de73e229f5879b04d$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: error.message
        }, {
            status: 500
        });
    }
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__c7bec341._.js.map